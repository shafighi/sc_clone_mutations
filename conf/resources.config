/*
================================================================================
  resources.config — cluster-wide resource caps
  Override these via --max_cpus / --max_memory / --max_time on the command line
  or by setting them in your institution's site profile.
================================================================================
*/

params {
    max_cpus   = 16
    max_memory = '128.GB'
    max_time   = '240.h'
}
